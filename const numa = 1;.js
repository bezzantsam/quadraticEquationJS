const numa = 1;
const numx = 10;
const numb = 10; 
const numc = 13.56;

console.log(Math.pow(numx, 2) * numa + (numb * numx) + numc);
/* the a in this formula is to the right of the x^2 part of the formula.*/ 